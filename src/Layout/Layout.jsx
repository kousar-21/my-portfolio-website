import React from 'react';
import Navbar from '../component/Navber/Navbar';

const Layout = () => {
    return (
        <div>
            <Navbar></Navbar>
        </div>
    );
};

export default Layout;